# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริงจาก LINE Developers Console
# (แท็บ Messaging API ของ Channel ที่สร้างไว้)

LINE_CHANNEL_ACCESS_TOKEN=ใส่ค่า_Channel_access_token_ตรงนี้
LINE_CHANNEL_SECRET=ใส่ค่า_Channel_secret_ตรงนี้
